apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ex03.fullname" . }}-test-connection"
  labels:
    {{- include "ex03.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ex03.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
